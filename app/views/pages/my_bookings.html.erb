<div class="container">
  <div class="card">
    <br>
    <h1>My Bookings (<%= current_user.email %>)</h1>
    </br>
    <ul>
      <% @bookings.each do |booking| %>
        <li><%= booking.start_date %>
            <%= booking.end_date %>
        </li>
      <% end %>
    </ul>
  </div>
</div>
